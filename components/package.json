{
  "name": "rune-reactor-components",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/rf-components.js --external:*.wasm",
    "watch": "esbuild src/index.ts --bundle --format=esm --outfile=dist/rf-components.js --external:*.wasm --watch"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}

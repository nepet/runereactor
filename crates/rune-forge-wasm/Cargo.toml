[package]
name = "rune-forge-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for rune-forge"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rune-forge = { path = "../rune-forge" }
wasm-bindgen = "0.2"
serde_json = "1"

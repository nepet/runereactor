[package]
name = "rune-reactor-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for rune-reactor"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rune-reactor = { path = "../rune-reactor" }
wasm-bindgen = "0.2"
serde_json = "1"
runeauth = "0.1"
hex = "0.4"

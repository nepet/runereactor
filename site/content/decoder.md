+++
title = "Decoder"
template = "page.html"
+++

# Rune Decoder

Paste a raw rune restriction string to see it broken down into human-readable components.

The raw format uses `&` to separate restrictions (AND) and `|` to separate alternatives within a restriction (OR). Each condition is a field name, an operator character, and a value.

<rf-decoder></rf-decoder>
